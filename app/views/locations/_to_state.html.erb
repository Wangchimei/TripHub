<%= f.fields_for :to_states do |f| %>
  <%= f.label :state_id %>
  <%= f.collection_select :state_id, states, :id, :name%>
<%end%>
  <div class="btn btn-sm" id="addCityBtn" style="display:none;">
    Show Cities
  </div>