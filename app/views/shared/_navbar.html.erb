<nav class="navbar navbar-expand-md navbar-dark nav-bg">
  <%=link_to image_tag( "logos/brand_logo_white.png", class:"nav-brand-image mr-2"), root_path%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent-4" aria-controls="navbarSupportedContent-4" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent-4">
    <ul class="navbar-nav">
      <li class="nav-item ml-2">
        <%= link_to "新規トリップ", new_trip_path, class:"nav-link #{"active" if current_page?(new_trip_path)}" %>
      </li>
      <li class="nav-item ml-2">
        <%= link_to "スケジュール探索", trips_path, class:"nav-link #{"active" if current_page?(trips_path)}" %>
      </li>
      <li class="nav-item ml-2">
        <%= link_to "スポット一覧", spots_path, class:"nav-link #{"active" if current_page?(spots_path)}" %>
      </li>
      <li class="nav-item ml-2">
        <%= link_to "スポット登録", new_spot_path, class:"nav-link #{"active" if current_page?(new_spot_path)}" %>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto nav-flex-icons">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= image_tag current_user.avatar, class:"nav-avatar" %>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
          <p class="dropdown-header">Hi, <%=current_user.name%></p>
          <%= link_to "ダッシュボード", dashboard_user_path(current_user), class:"dropdown-item #{"dropdown-active" if current_page?(dashboard_user_path(current_user))}" %>
          <%= link_to "マイトリップ", trips_user_path(current_user), class:"dropdown-item #{"dropdown-active" if current_page?(trips_user_path(current_user))}" %>
          <%= link_to "プロフィール", user_path(current_user), class:"dropdown-item #{"dropdown-active" if current_page?(user_path(current_user))}" %>
          <div class="dropdown-divider"></div>
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class:"dropdown-item" %>
        </div>
      </li>
    </ul>
  </div>
</nav>