$('.calendar').fullCalendar('removeEvents', [<%= @schedule.id %>]);
$('.calendar').fullCalendar(
  'renderEvent',
  $.parseJSON("<%=j render(@schedules, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');