<h1>My Trip</h1>
<% @trips.each do |trip| %>
  <p><%= trip.name %>
  <%= trip.description %>
  <%= trip.start_day %> to <%= trip.end_day %>
  <%= (trip.end_day-trip.start_day).to_i + 1 %> 日
  <%= link_to 'Details', trip_path(trip)%>
  </p>
<%end%>