<h1>Trips#show</h1>
  <p><%= @trip.name %>
  <%= @trip.description %>
  <%= @trip.start_day %> to <%= @trip.end_day %>
  <%= trip_duration %> 日 </p>

  <% @trip.dailies.each do |daily| %>
    <p>
    <%= daily.date %>
    <%= link_to 'Edit', edit_trip_daily_path(@trip, daily) %>
    <%= daily.start_time.strftime("%R") %>
    <%= daily.end_time.strftime("%R") %>
    </p>
  <%end%>